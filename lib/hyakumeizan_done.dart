import 'package:flutter/material.dart';

class HyakumeizanDone extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("登った百名山一覧"),
      ),
      body: Center(
          child: Text('Comming update...'),
        ),
      );
  }
}